# Archived: Flutter prototype

Previous cross-platform implementation using Flutter.

The production app now uses **Expo (React Native + TypeScript)** at the repo root — a more widely adopted mobile stack with Expo Go for quick Android testing and EAS for store builds.
